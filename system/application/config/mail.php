<?php if (!defined('BASEPATH')) exit('No direct script access allowed');

// The mail server specified here will only work from hosts which have access.
// This could be changed to be more like the google.php config file if need be.
// We may also need more options in here for mail servers which need
// authentication to send mail.

$config['mail']['smtp_server'] = 'theyorker.co.uk'

?>
